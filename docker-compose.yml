version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: data-engine-frontend
    restart: unless-stopped
    ports:
      - "9601:9601"
    environment:
      - NODE_ENV=production
